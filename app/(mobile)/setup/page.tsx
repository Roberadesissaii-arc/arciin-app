import { MobileSetupPage } from "@/components/auth/mobile-setup-page"

/** First-run claim for a co-located, unclaimed Arciin server (token auto-fills). */
export default function SetupPage() {
  return <MobileSetupPage />
}
